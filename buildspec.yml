version: 0.1
phases:
  install:
    commands:
      - npm install time
      - aws cloudformation package --template-file template.yml --s3-bucket ippevent-public
                                   --output-template-file SamTemplate.yml
artifacts:
  type: zip
  files:
    - SamTemplate.yaml
